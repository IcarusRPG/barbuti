{
  "name": "barbutti-monorepo",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "setup": "pnpm install && pnpm --filter @barbutti/api prisma:generate && pnpm --filter @barbutti/api prisma:migrate && pnpm --filter @barbutti/api prisma:seed"
  }
}
